# Copy this file to .env and update the values

# Database connection string (will be provided by Railway PostgreSQL service)
DATABASE_URL=postgresql://username:password@host:port/database

# Node environment
NODE_ENV=production

# API Port (Railway will set this automatically)
PORT=3001
